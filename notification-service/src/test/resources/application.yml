spring:
  profiles:
    include: common
  rabbitmq:
    host: 127.0.0.1
    username: guest
    password: guest

  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

redis:
  host: localhost
  port: 6379
  database: 0
